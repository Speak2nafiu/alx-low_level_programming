#include <stdio.h>

/**
 * _strcpy - prints buffer in hexa
 * @dest: the address of memory to print
 * @src: the size of the memory to print
 *
 * Return: Nothing.
 */

char *_strcpy(char *dest, char *src)
{
	return (dest);
}
