#include <stdoi.h>
#include "calc.h"

/**
 *main - defines all code
 *op_add: adiition
 *op_sub - subtraction
 *Return: always 0.
 *op_mul - multiplicatiocation
 *op_div - division
 *op_mod - modulus
 */

void main(void)
{
	int op_add(int a, int b)
	{
		return (a + b);
	}

	int op_sub(int a, int b)
	{
		return (a - b);
	}

	int op_mul(int a, int b)
	{
		return (a * b);
	}

	int op_div(int a, int b)
	{
		return (a / b);
	}
	int op_mod(int a, int b)
	{
		return (a % b);
	}
	return (void);
}
