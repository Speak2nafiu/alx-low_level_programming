#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 *_strlen_recursion - finds the string lenght
 *
 *@s: stringto work on
 * Return: always 0
 */

int _strlen_recursion(char *s)
{
	int len = strlen(s);

	return (len);
}


