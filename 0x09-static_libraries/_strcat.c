#include <stdio.h>


/**
 * _strcat - prints buffer in hexa
 * @dest: the address of memory to print
 * @src: the size of the memory to print
 *
 * Return: Nothing.
 */

char *_strcat(char *dest, char *src)
{
	return (dest);
}
