#include <stdio.h>

/**
 * _strncat - prints buffer in hexa
 * @dest: the address of memory to print
 * @src: the size of the memory to print
 * @n: interger
 * Return: Nothing.
 */

char *_strncat(char *dest, char *src, int n)
{
	return (dest);
}
