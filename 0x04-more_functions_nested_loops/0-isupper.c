#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *_isupper - returns 1 for capital and 0 for  lower case
 * Return: Always 0
 *
 */

int _isupper(int c)
{
	if (_isupper(c) > 0)
	{
		return (1);

	}

	else
		{
		return (0);

		}

	return (0);
}
