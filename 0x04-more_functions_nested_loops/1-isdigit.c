#include "main.h"
#include <stdio.h>

/**
 * _isdigit - check the code
 *
 * c - input value
 *
 * Return: Always 0.
 */


int _isdigit(int c )
{
	if (_isdigit(c))
	{
		return (1);
	}

	else
	{
		return (0);
	}

	return (0);
}
